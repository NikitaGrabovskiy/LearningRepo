            Here are the things that I need to know perfectly.
            Practice the list in order every marathon day

1) Build Spring boot app:
            * Rest API 
            * Spring Data JPA 
            * 3 Tests - @SpringBootTest, @DataJpaTest, @WebMvcTest

2) Practice streams:

    1.Convert all strings in a list to uppercase
    2.Remove duplicate strings from a list and sort the result
    3.Square each number in a list and calculate the total sum
    4.Convert a list of strings into a map where the key is the string and the value is its length
    5.Group strings by their length
    6.Find the longest string in a list
    7.Join all strings into a single comma-separated string
    8.Filter objects based on a condition and extract a specific field into a list
    9.Partition objects into two groups based on a boolean condition 
   10.Generate a range of integers and count how many even numbers exist


3) Practice threads
    * Create thread by extending thread class
    * Create Runnable and Callable and run using executor service
    * Practice syncronized resource

4) Create Comparator and Comparable and practice sorting list in a collection

5) Create a SpringBoot application and secure endpoints with tokens and spring security

6) Practice SQL queries on website : https://sqlzoo.net/wiki/SQL_Tutorial (Starting from 4 SELECT within SELECT)
