
                                    *** Convert character to integer ***

int digit = Character.getNumericValue(ch);

                                    *** Convert Integer to

public List<Integer> getDigits(int n) {
    List<Integer> list = new ArrayList<>();

    while (n > 0) {
        list.add(n % 10);
        n /= 10;
    }

    Collections.reverse(list); // to keep original order

    return list;
}